## Call multiline 1 pre existing line breaks

Original php code:

```php
return strtolower(
  'We put so many words into this text, that it does not fit into one line.',
);
```

Prettified code:

```php
return strtolower(
  'We put so many words into this text, that it does not fit into one line.',
);
```
