{#
/**
 * @file
 * Default theme implementation of a container used to wrap child elements.
 *
 * Traditionally, in Drupal 7, themekit had its own version of the 'container'
 * theme hook, as a more simple option.
 * This may no longer be needed in Drupal 10.
 *
 * Available variables:
 * - attributes: HTML attributes for the containing element.
 * - children: The rendered child elements of the container.
 * - tag_name: Tag name to use. Defaults to 'div'.
 *
 * @see template_preprocess_container()
 *
 * @ingroup themeable
 */
#}
{%- set _content %}{{ element['#children'] }}{% endset -%}
{%- if _content|trim != '' -%}
  {{- {
    '#type': 'html_tag',
    '#tag': element['#tag_name'] ?? 'div',
    '#value': _content,
    '#attributes': attributes,
  } -}}
{%- endif -%}
